from util.table import Table

class __Classes():
    name = "classes"
    columns = {
            "class_code": str,
            "expires_at": str,
            "subject_id": int,
            "prof_id": int,
    }

Classes = __Classes()
