from util.table import Table

class __Attendance():
    name = "attendance"
    columns = {
            "class_code": str,
            "sr_code": str,
            "created": str,
    }

Attendance = __Attendance()
