from util.table import Table

class __Subjects():
    name = "subjects"
    columns = {
            "code": str,
            "name": str,
            "prof_id": int,
            "created_at": str,

    }


Subjects = __Subjects()
